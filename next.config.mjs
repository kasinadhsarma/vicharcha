/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['placeholder.svg', 'placeholder-user.jpg', 'placeholder-album.jpg'],
  },
  reactStrictMode: true,
  swcMinify: true,
};

export default nextConfig;
